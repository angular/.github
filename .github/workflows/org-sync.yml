name: Org File Sync

on:
  workflow_dispatch:
  push:
    branches:
      - main

# Declare default permissions as read only.
permissions: read-all

jobs:
  org_file_sync:
    runs-on: ubuntu-latest
    steps:
      - uses: angular/dev-infra/github-actions/org-file-sync@fc83b85eca42d5ad36e7f2875892b13d6adc5d1b
        with:
          angular-robot-key: ${{ secrets.ANGULAR_ROBOT_PRIVATE_KEY }}
          repos: |
            angular
            angular-cli
            components
            code-of-conduct
            dev-infra
            universal
            vscode-ng-language-service
          files: |
            CODE_OF_CONDUCT.md
            SECURITY.md
